# 腰有据 · 开发约定

需求见 `docs/brief.md`，系统设计见 `docs/system-design.md`，设计稿与设计规范见 `docs/design/`，任务总览见 `PLAN.md`，任务明细在 `todo/` 下。每次开始工作前先读这些文件。

## 设计还原
1. 界面以 `docs/design/` 中对应编号的 PNG 为准，逐页还原布局、颜色、字号、间距、图标和文案。做页面前先打开对应的设计稿图片。
2. 颜色、字号、圆角、状态标签按 `docs/design/README.md` 的设计规范，写成各端的主题变量，不要在页面里散落硬编码的颜色值。
3. 图标自绘（SVG）或使用本地图标库，不加载外部资源。
4. 产品红线和全局约定见 `docs/brief.md`，所有端都必须遵守。

## 工作流程（每个任务都必须遵守）
1. 任务在 `todo/` 下，一个任务一个文件。按编号顺序做，一次只做一个。
2. 按任务文件中的“完成标准”自查，并运行该子项目的构建 / 类型检查 / 测试，全部通过才算完成。
3. 任务完成后依次执行：
   1. 把任务文件中的 `状态：待完成` 改为 `状态：已完成（vX.Y，YYYY-MM-DD）`；
   2. 用 `git mv` 把任务文件移到 `todo/completed/`；
   3. 在本地提交：`git commit -m "vX.Y 本次工作内容"`；
   4. 推送到远程：`git push`。
4. 版本号：先用 `git log` 查最近一次提交的版本号，把次版本号加 1 作为本次版本号（例如 v0.9 之后是 v0.10）。
5. 提交信息只写一行：版本号 + 一个空格 + 不超过 30 个字的中文说明，写清本次做了什么。不写正文，不写多行。
6. 一次提交只包含一个任务的改动。
7. 不要提交 `node_modules`、构建产物、数据库文件、日志和 `.env`。任何文件、命令和提交信息中都不能出现账号、密码、令牌或 API Key。
8. 推送失败时重试一次；仍然失败，就在汇报中写明原因，继续下一个任务，下次推送时一起推上去。不要修改远程地址，不要配置或输入任何凭据。
9. 按顺序完成 `todo/` 中的全部任务。中途不要停下来等待确认，除非遇到无法自行解决的阻塞；全部完成后汇报：完成了哪些任务、版本号范围、怎么启动、还有哪些已知问题。

## 本机运行约定
本机可能同时运行着其他项目，请遵守：
1. 只在本仓库目录内读写文件，不要读取或修改仓库以外的目录。
2. 本项目只使用以下端口：server 3200、app（H5） 5201、web 5202、admin 5203。启动服务前先确认端口空闲，不要占用其他端口。
3. 只结束自己启动的进程（按进程 ID，或按本项目端口查到的进程 ID）。不要按进程名批量结束进程（例如 `taskkill /IM node.exe`），那会影响本机的其他程序。
4. 已经推送的提交不要改写，不要使用 `git push --force`；发现问题时，用一个新的提交修正。
